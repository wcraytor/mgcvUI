% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trilogy.R
\name{trilogy_set_lock}
\alias{trilogy_set_lock}
\title{Lock the canonical earth model for a Trilogy run}
\usage{
trilogy_set_lock(
  project_path,
  fit_ts,
  rds,
  downstream_locked = list(),
  shared = NULL
)
}
\arguments{
\item{project_path}{Absolute path to the regProj appraisal project (the flat
segment folder under \verb{<root>/appr/}).}

\item{fit_ts}{Fit-stamp string of the locked earth fit (\code{"\%Y\%m\%d_\%H\%M\%S"}).}

\item{rds}{Path to the locked earth result \code{.rds} (relative to the project,
or absolute).}

\item{downstream_locked}{Named list of settings locked downstream (e.g.
\code{predictors}, \code{factor}, \code{linear}).}

\item{shared}{Optional named list of shared settings (target, \code{cqa_mode},
\code{cqa}, ...); left unchanged when \code{NULL}.}
}
\value{
Invisibly, the path written.
}
\description{
Records which earth \code{.rds} (and its fit-stamp) is the one glmnetUI and mgcvUI
import, along with the settings that locking earth imposes downstream and the
shared settings (target, CQA, raw/per-SF). Mirrors today's earth-import lock
plus the CQA additions.
}
