% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regproj.R
\name{regproj_flat_segment}
\alias{regproj_flat_segment}
\title{Encode the flat regProj segment from components}
\usage{
regproj_flat_segment(country, levels, project_name)
}
\arguments{
\item{country}{Lowercase ISO 3166-1 alpha-2 country code (e.g. \code{"us"}).}

\item{levels}{Character vector of admin codes, ordered top to bottom.
Length must match \code{length(country_schema(country))}. Each element
must satisfy \verb{^[a-z0-9-]+$} (no underscores — they are the segment
separator).}

\item{project_name}{Project leaf name. Must satisfy \verb{^[A-Za-z0-9_-]+$},
max 24 characters. Underscores are allowed because the parser uses
the country schema to know how many tokens are admin vs project.}
}
\value{
Character scalar (e.g. \code{"us_ca_081_burlin_20251231_j"}).
}
\description{
Joins country, admin levels, and project name with \verb{_} to produce the
single hierarchy-encoding folder name used at the project level under
\verb{<root>/<purpose>/}. Validates each component: admin codes must match
\verb{^[a-z0-9-]+$} (no internal underscores), project name allows
\verb{^[A-Za-z0-9_-]+$}.
}
