% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trilogy.R
\name{trilogy_write_conclusion}
\alias{trilogy_write_conclusion}
\title{Write a method's value conclusion as \verb{conclusion_<fit_ts>.json}}
\usage{
trilogy_write_conclusion(
  out_dir,
  method,
  fit_ts,
  subject_value,
  metrics = list(),
  extra = list()
)
}
\arguments{
\item{out_dir}{Directory to write into (the method's output folder).}

\item{method}{One of \code{"earth"}, \code{"glmnet"}, \code{"mgcv"}.}

\item{fit_ts}{Fit-stamp string (\code{"\%Y\%m\%d_\%H\%M\%S"}).}

\item{subject_value}{The subject property's indicated value.}

\item{metrics}{Optional named list of fit metrics (e.g. \code{r2}, \code{rmse}).}

\item{extra}{Optional named list of extra fields to include.}
}
\value{
Invisibly, the path written.
}
\description{
Each app writes one of these into its own output folder so the combined
report can pick up the subject value + key metrics by fit timestamp. The
filename embeds \code{fit_ts} (matching the rest of the fit's outputs).
}
