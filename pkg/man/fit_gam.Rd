% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_gam.R
\name{fit_gam}
\alias{fit_gam}
\title{Fit a GAM Model}
\usage{
fit_gam(
  data,
  response,
  smooth_specs,
  family = gaussian(),
  method = "REML",
  earth_knots = NULL,
  select = TRUE,
  gamma = 1.2,
  cv_folds = 10L,
  weights = NULL,
  optimizer = NULL,
  scale = 0,
  discrete = FALSE,
  nthreads = 1L
)
}
\arguments{
\item{data}{Data frame of observations.}

\item{response}{Character name of the response variable.}

\item{smooth_specs}{List of smooth-term specifications (see
\code{\link[=build_gam_formula]{build_gam_formula()}}).}

\item{family}{A family object or character string (e.g. \code{"gaussian"},
\code{"Gamma"}). Default is \code{gaussian()}.}

\item{method}{Character fitting method. Default \code{"REML"}.}

\item{earth_knots}{Optional \code{mgcvUI_earth_knots} object from
\code{\link[=import_earth]{import_earth()}}. When provided, knot positions are passed to
\code{\link[mgcv:gam]{mgcv::gam()}}.}

\item{select}{Logical. If \code{TRUE}, adds an extra penalty to each
smooth term so that it can be penalised to zero (variable
selection). Default \code{FALSE}.}

\item{gamma}{Numeric inflation factor for the effective degrees of
freedom in the GCV/UBRE score. Values greater than 1 produce
smoother fits. Default \code{1}.}

\item{cv_folds}{Integer number of cross-validation folds. Set to 0
to skip CV. Default \code{10}.}

\item{weights}{Optional numeric vector of prior weights (one per
observation). Passed to \code{\link[mgcv:gam]{mgcv::gam()}}.}

\item{optimizer}{Character optimizer key: \code{"outer_newton"} (default),
\code{"outer_bfgs"}, or \code{"efs"}.}

\item{scale}{Numeric scale parameter. \code{0} (default) means estimate;
a positive value fixes it.}

\item{discrete}{Logical. If \code{TRUE}, uses discretized covariate
method for faster fitting on large datasets. Default \code{FALSE}.}

\item{nthreads}{Integer number of threads for parallel computation.
Default \code{1}.}
}
\value{
An \code{mgcvUI_result} object (a list) with components:
\describe{
\item{model}{The fitted \link[mgcv:gam]{mgcv::gam} object.}
\item{formula}{The formula used.}
\item{response}{Response variable name.}
\item{smooth_specs}{The smooth-term specs used.}
\item{earth_knots}{The earth knots used (or \code{NULL}).}
\item{elapsed}{Fitting time in seconds.}
\item{cv_rsq}{Cross-validated R-squared (or \code{NULL} if CV skipped).}
}
}
\description{
Wrapper around \code{\link[mgcv:gam]{mgcv::gam()}} that accepts the specification objects
produced by the mgcvUI interface and returns a structured result.
}
\examples{
specs <- list(
  list(vars = "wt", type = "s", bs = "tp", k = NULL),
  list(vars = "hp", type = "s", bs = "tp", k = NULL)
)
result <- fit_gam(mtcars, "mpg", specs)
summary(result$model)
}
