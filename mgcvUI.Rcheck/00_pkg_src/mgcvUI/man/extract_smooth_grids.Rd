% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_functions.R
\name{extract_smooth_grids}
\alias{extract_smooth_grids}
\title{Extract Smooth Functions as Lookup Tables}
\usage{
extract_smooth_grids(gam_result, n_points = 200L)
}
\arguments{
\item{gam_result}{An \code{mgcvUI_result} from \code{\link[=fit_gam]{fit_gam()}}.}

\item{n_points}{Integer number of grid points per variable.
Default \code{200}.}
}
\value{
A named list of data frames, one per smooth variable.
Each has columns \code{x} and \code{fx}.
}
\description{
Evaluates each smooth term on a fine grid and returns x/fx pairs
suitable for interpolation in any language.
}
\examples{
specs <- list(
  list(vars = "wt", type = "s", bs = "tp", k = NULL),
  list(vars = "hp", type = "s", bs = "tp", k = NULL)
)
res <- fit_gam(mtcars, "mpg", specs, cv_folds = 0)
grids <- extract_smooth_grids(res, n_points = 50)
names(grids)
}
