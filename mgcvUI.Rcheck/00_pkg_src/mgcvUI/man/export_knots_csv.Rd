% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import_earth.R
\name{export_knots_csv}
\alias{export_knots_csv}
\title{Export Knot Positions to CSV}
\usage{
export_knots_csv(earth_knots, file)
}
\arguments{
\item{earth_knots}{An \code{mgcvUI_earth_knots} object from
\code{\link[=import_earth]{import_earth()}}.}

\item{file}{Character path for the output CSV file.}
}
\value{
The file path (invisibly).
}
\description{
Writes a tidy CSV with one row per knot, columns: variable, knot,
direction.
}
\examples{
if (requireNamespace("earth", quietly = TRUE)) {
  m <- earth::earth(mpg ~ wt + hp, data = mtcars)
  er <- structure(
    list(model = m, target = "mpg",
         predictors = c("wt", "hp"),
         categoricals = character(0), linpreds = character(0),
         degree = 1L, cv_enabled = FALSE, allowed_matrix = NULL,
         data = mtcars, elapsed = 0, trace_output = character(0)),
    class = "earthUI_result"
  )
  knots <- import_earth(er)
  tmp <- tempfile(fileext = ".csv")
  export_knots_csv(knots, tmp)
  cat(readLines(tmp), sep = "\n")
  unlink(tmp)
}
}
