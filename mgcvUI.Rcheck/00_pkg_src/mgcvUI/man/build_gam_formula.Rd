% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_gam_formula.R
\name{build_gam_formula}
\alias{build_gam_formula}
\title{Build a GAM Formula from Variable Specifications}
\usage{
build_gam_formula(response, smooth_specs)
}
\arguments{
\item{response}{Character scalar -- the response variable name.}

\item{smooth_specs}{A list of smooth-term specification lists. Each
element should have:
\describe{
\item{vars}{Character vector of variable names (length 1 for
univariate smooths, 2+ for tensor products).}
\item{type}{\code{"s"}, \code{"te"}, \code{"ti"}, or \code{"linear"}.}
\item{bs}{Basis type: \code{"tp"}, \code{"cr"}, \code{"ps"}, \code{"bs"}, etc.
Ignored for \code{"linear"} and tensor types.}
\item{k}{Integer basis dimension, or \code{NULL} for automatic.}
}}
}
\value{
A \link[stats:formula]{stats::formula} object.
}
\description{
Constructs a \link[stats:formula]{stats::formula} suitable for \code{\link[mgcv:gam]{mgcv::gam()}} from a list
of smooth-term specifications.
}
\examples{
specs <- list(
  list(vars = "wt", type = "s", bs = "cr", k = 5L),
  list(vars = "hp", type = "s", bs = "tp", k = NULL),
  list(vars = "cyl", type = "linear", bs = NULL, k = NULL)
)
build_gam_formula("mpg", specs)
}
