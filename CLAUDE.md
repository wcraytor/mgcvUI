# CLAUDE.md — mgcvUI

Project-level instructions for Claude Code when working on this repository.

## Project Overview

**mgcvUI** is an R CRAN package providing a Shiny-based GUI for
Generalized Additive Models using [mgcv](https://CRAN.R-project.org/package=mgcv).

## Repository Layout

```
R/                        # Package source
  launch.R                # mgcvUI() — starts Shiny app
  regproj.R               # regProj project model (paths, geo helpers, prefs)
  regproj_db.R            # geo.sqlite + projects.sqlite backends (shared root)
  mod_data.R              # Import module — lists/loads files from project in/
  mod_earth_import.R      # earthUI result (.rds) import module
  mod_model.R             # GAM fitting module
  mod_variables.R         # Variable configuration module
  mod_report.R            # Legacy report module (not surfaced in the app UI)
  build_gam_formula.R     # GAM formula builder
  fit_gam.R               # GAM fitting wrapper (mgcvUI_result)
  format_results.R        # Equation, summary formatters
  plot_results.R          # Plot functions
  import_data.R           # CSV/Excel import (with memory check)
  import_earth.R          # earthUI result parser
  export_report.R         # Two-step Quarto report + legacy rmarkdown/officer
  export_functions.R      # Function export utilities
  detect_types.R          # Column type detection
  locale.R                # Country-based locale system
  settings_db.R           # Local settings.sqlite (locale defaults)
inst/
  app/
    app.R                 # Combined UI + server (Bootstrap 5, Nord theme)
    sales_grid.R          # Sales Comparison Grid generator
  quarto/
    gam_report.qmd        # Quarto report template (HTML/Word/PDF)
    reference.docx        # Word styling reference
  extdata/
    regproj_reference.json, regproj_geo.rds   # geo DB seed data
  rmd/gam_report.Rmd      # Legacy rmarkdown report template
tests/testthat/           # testthat tests
DESCRIPTION               # Package metadata
NAMESPACE                 # Auto-generated by roxygen2
```

## regProj project model

mgcvUI shares one project tree and two SQLite databases with the sibling apps
(earthUI, glmnetUI), rooted at `~/regProj` (override via the `REGPROJ_ROOT`
env var or the regProj-root field in Settings → persisted to
`R_user_dir("mgcvUI","config")/prefs.json`).

- `geo.sqlite` — country/admin-code cascades (seeded from `inst/extdata`).
- `projects.sqlite` — one row per project + per-(project, purpose) settings in
  per-method columns (`mgcv_settings`/`mgcv_variables`). mgcvUI uses
  `method = "mgcv"` and writes outputs to `<project>/<os>_out_mgcv`.
- Schema/migrations are idempotent and identical across apps — whichever app
  connects first creates/migrates; the others no-op. **Do not** diverge the
  `regproj_db.R` schema from earthUI's.
- The Purpose radio and output-folder input are hidden in the DOM and driven
  by the active project (so existing `input$purpose` / `input$output_folder`
  readers keep working).

## Build & Test

```bash
# Run the Shiny app (port 7880) via devtools
Rscript -e 'devtools::load_all(); mgcvUI(launch.browser = TRUE)'

# Install package locally (never use quick=TRUE — it skips inst/ updates)
R CMD INSTALL .

# Rebuild roxygen docs + NAMESPACE
Rscript -e 'roxygen2::roxygenise()'

# Run tests (use devtools::test, which load_all()s the package first;
# testthat::test_dir does not auto-load and will fail on internal symbols)
Rscript -e 'devtools::test()'
```

## Key Conventions

- **Purpose modes**: Radio button (`input$purpose`) with three options:
  `"general"`, `"appraisal"`, `"market"`. Last-used purpose persisted
  via localStorage (`mgcvUI_last_purpose`).
- **Earth import**: Uses `shinyFiles::shinyFilesButton` for browsing
  earthUI result files.
- **Dark mode**: Toggle button with `session$setCurrentTheme()`.
- **CSS classes**: Use `mgcv-` prefix for custom classes.
- **Port**: Fixed port 7880.

## Shared UI Conventions (earthUI, glmnetUI, mgcvUI)

These conventions apply consistently across all three sibling apps:

- **Nord Theme**: All UI colors must come from the official Nord palette
  (nord0–nord15). See https://www.nordtheme.com/docs/colors-and-palettes.
  Light mode uses Snow Storm base (nord6 bg, nord0 fg). Dark mode uses
  Polar Night base (nord0 bg, nord4 fg). Frost for accents, Aurora only
  for warnings/errors (alerts, validation messages), never on buttons.
- **Button classes**: Interactive buttons use Frost colors only:
  `btn-primary`, `btn-secondary`, `btn-outline-primary`,
  `btn-outline-secondary`. Never use `btn-success`, `btn-danger`,
  `btn-outline-success`, or `btn-outline-danger` on buttons.
- **Purpose persistence**: Last-used purpose is saved to localStorage
  (`mgcvUI_last_purpose`) on change and restored on `shiny:connected`.
  Each app uses its own key so they persist independently.
- **sales_grid.R exception**: Excel export colors follow third-party
  conventions, not the Nord palette.

## Common Pitfalls

- Never use `devtools::install(quick = TRUE)` — it skips `inst/` file
  updates.
- Kill port 7880 before relaunching: `lsof -ti:7880 | xargs kill`.
- **R `tryCatch` scoping bug**: `doc <- ...` inside `tryCatch({...})`
  silently writes to a local copy when nested inside `for` loops
  within Shiny `observeEvent` callbacks. The outer variable is never
  updated — no error, no warning. **Workaround**: never mix mutable
  state (`doc <-`) with `tryCatch`. Instead, do pure
  computation/file-generation inside `tryCatch` (return data or
  NULL), then do all state updates (`doc <-`) in straight-line code
  outside the `tryCatch`. This applies to all three sibling apps.
- **Shiny scoping traps**: `observeEvent`, `observe`, `reactive`,
  `renderUI`, and `renderPlot` all create function scopes.
  Assigning to a local variable inside these (especially when
  nested with `tryCatch` or `for` loops) silently creates a local
  copy — the outer scope is never updated. Use `reactiveVal` or
  `reactiveValues` for shared state, and call `rv(new_value)` not
  `rv <- new_value` (the latter silently overwrites the reactive
  with a plain value). Same workaround: keep `tryCatch` for pure
  computation only; do all side effects in straight-line code.
