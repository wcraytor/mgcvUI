% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_results.R
\name{plot_smooth_single}
\alias{plot_smooth_single}
\title{Plot a Single GAM Smooth Term}
\usage{
plot_smooth_single(gam_result, variable, residuals = TRUE, earth_knots = NULL)
}
\arguments{
\item{gam_result}{An \code{mgcvUI_result} from \code{\link[=fit_gam]{fit_gam()}}.}

\item{variable}{Character name of the variable to plot.}

\item{residuals}{Logical. Overlay partial residuals? Default \code{TRUE}.}

\item{earth_knots}{Optional \code{mgcvUI_earth_knots} object. If provided,
earth knot positions are marked on the plot as vertical dashed
lines.}
}
\value{
A ggplot object.
}
\description{
Plots one smooth term from the fitted GAM with confidence band and
optional partial residuals.
}
\examples{
specs <- list(
  list(vars = "wt", type = "s", bs = "tp", k = NULL),
  list(vars = "hp", type = "s", bs = "tp", k = NULL)
)
res <- fit_gam(mtcars, "mpg", specs)
plot_smooth_single(res, "wt")
}
