% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regproj.R
\name{regproj_new_project_name}
\alias{regproj_new_project_name}
\title{Build a timestamped project-name segment for a NEW project}
\usage{
regproj_new_project_name(name, when = Sys.time())
}
\arguments{
\item{name}{User-typed project name. Must match \verb{^[A-Za-z0-9_-]+$} and be at
most 8 characters.}

\item{when}{Creation time. Defaults to \code{\link[=Sys.time]{Sys.time()}}; pass a fixed value in
tests for reproducibility.}
}
\value{
Character scalar \verb{<YYYYMMDD-HHMMSS>_<name>}.
}
\description{
New projects auto-prepend a creation timestamp to the user-supplied name so
every project folder is unique and time-ordered. The user-typed portion is
validated: it must match \verb{^[A-Za-z0-9_-]+$} and be at most 8 characters.
The result \verb{<YYYYMMDD-HHMMSS>_<name>} becomes the project-name segment of the
regProj flat path, i.e. the full folder is
\verb{<country>_<admin codes...>_<YYYYMMDD-HHMMSS>_<name>}.
}
\details{
This is country-agnostic: it does not depend on how many admin levels a
country has (see \code{\link[=country_schema]{country_schema()}}), so it works for every political naming
convention — US \code{state/county/city}, FR \code{region/departement/commune},
SG \code{planning_area}, the generic \code{region/city} fallback, etc. Existing
projects are untouched; only newly created ones get the prefix.
}
