% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regproj.R
\name{regproj_path}
\alias{regproj_path}
\title{Build a regProj-canonical path}
\usage{
regproj_path(
  purpose,
  country,
  levels,
  project_name,
  os = os_detect(),
  in_or_out = c("out", "in"),
  method = "mgcv",
  root = default_regproj_root(),
  create = FALSE
)
}
\arguments{
\item{purpose}{\code{"gen"}, \code{"appr"}, or \code{"mktarea"}.}

\item{country}{Lowercase ISO 3166-1 alpha-2 country code (e.g. \code{"us"}).}

\item{levels}{Character vector of admin codes, ordered top to bottom.
Length must match \code{length(country_schema(country))}. Each element
must satisfy \verb{^[a-z0-9-]+$} (no underscores — they are the segment
separator).}

\item{project_name}{Project leaf name. Must satisfy \verb{^[A-Za-z0-9_-]+$},
max 24 characters. Underscores are allowed because the parser uses
the country schema to know how many tokens are admin vs project.}

\item{os}{One of \code{"mac"}, \code{"ubuntu"}, \code{"win11"}. Defaults to
\code{\link[=os_detect]{os_detect()}}.}

\item{in_or_out}{One of \code{"in"} or \code{"out"}. Defaults to \code{"out"}.}

\item{method}{Optional method subdir (e.g. \code{"earth"}, \code{"glmnet"},
\code{"mgcv"}, \code{"combined"}). When \code{in_or_out = "out"} and \code{method} is
non-empty, the leaf becomes \verb{<os>_out_<method>}. Ignored for \code{"in"}.
Defaults to \code{"mgcv"} for this package.}

\item{root}{Optional explicit regProj root. Defaults to
\code{\link[=default_regproj_root]{default_regproj_root()}}.}

\item{create}{Logical. If \code{TRUE}, the directory tree is created with
\code{dir.create(recursive = TRUE)}. Defaults to \code{FALSE}.}
}
\value{
Character scalar. Absolute normalized path.
}
\description{
Composes the path
\verb{<root>/<purpose>/<flat_segment>/<os>_<in|out>[_<method>]} from its
components. The hierarchy (country / admin levels / project name) is
concatenated into a single folder under \verb{<purpose>/} to keep the tree
shallow. Pure path computation — does not create any directories
unless \code{create = TRUE}.
}
